<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BlankApp1.Views.MainPage"
             xmlns:local="clr-namespace:BlankApp1.Views"
             Title="{Binding Title}">

    

    <StackLayout>
        
        <StackLayout Orientation="Horizontal" VerticalOptions="Start">
            <!-- top controls -->

            

            <Button x:Name="btn1" Text="btn1" Command="{Binding btn1Click}"/>
            <Button x:Name="btn2"  Text="btn2" Command="{Binding btn2Click}"/>
            <Button x:Name="btn3"  Text="btn3" Command="{Binding btn3Click}"/>

        </StackLayout>

        <StackLayout VerticalOptions="CenterAndExpand" x:Name="EntryFieldsStack" BindingContext="">
           

        </StackLayout>

        <StackLayout Orientation="Horizontal" VerticalOptions="End">
           
            <Label Text="Đây là bottom"/>

        </StackLayout>
    </StackLayout>

</ContentPage>