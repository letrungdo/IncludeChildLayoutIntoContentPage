<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BlankApp1.Views.MainPage"
             xmlns:local="clr-namespace:BlankApp1.Views"
             Title="{Binding Title}">

    

    <StackLayout>
        
        <StackLayout Orientation="Horizontal" VerticalOptions="Start">
            <!-- top controls -->
        </StackLayout>

        <StackLayout VerticalOptions="CenterAndExpand">
            <!--
            <Label Text="Btn 1 isClick" HorizontalOptions="Center" IsVisible="{Binding IsVisibleLbl1}"/>
            <Label Text="Btn 2 isClick" HorizontalOptions="Center" IsVisible="{Binding IsVisibleLbl2}"/>
            
            -->

        </StackLayout>

        <StackLayout Orientation="Horizontal" VerticalOptions="End">
            <!-- bottom controls 
            <Button Text="btn1" Command="{Binding btn1Click}"/>
            <Button Text="btn2" Command="{Binding btn2Click}"/>
            <Button Text="btn3" Command="{Binding btn3Click}"/>
            -->

            <local:Bar>

            </local:Bar>

        </StackLayout>
    </StackLayout>

</ContentPage>